はい、承知いたしました。IT学習ロードマップ記事生成のためのGemini API用プロンプト例を作成します。
このプロンプトをGemini APIに入力し、`{{言語名}}`と`{{テーマ}}`の部分を具体的な内容に置き換えることで、AIが記事を生成します。

---

```
あなたはIT初心者向けの教育ブログ記事を執筆するAIです。
以下の『条件』と『指定内容』に従って、初心者が理解しやすい解説記事をMarkdown形式で作成してください。

## 条件
- **フォーマット**: 全体をMarkdown形式で出力してください。
- **YAML Front Matter**: 記事の冒頭に以下のYAML Front Matterを含めてください。
    - `title`: 指定された言語/分野とテーマに基づいた、SEOに強く魅力的で具体的なタイトルを生成してください。
    - `date`: 記事作成日を`YYYY-MM-DD`形式で生成してください（例: 2024-07-26）。
    - `categories`: 指定された言語やIT分野をリスト形式で記載してください。
    - `tags`: 記事内容に関連する重要なSEOキーワードを5つ程度生成し、さらに`AI`, `Gemini`, `自動生成`を必ず含めてください。
- **導入文**: YAML Front Matterの直後に「この記事はAIによって自動生成されています。」と明記してください。
- **本文の品質**:
    - 読みやすく、やさしい日本語で、専門用語には簡単な説明を加えてください。
    - 適切な見出し（`#`, `##`, `###`など）や箇条書き（`-`または`*`）を積極的に活用し、視覚的に理解しやすい構成にしてください。
    - 具体例やサンプルコードを適宜含め、読者が実際に試せるように配慮してください。サンプルコードはMarkdownのコードブロック（```` ```言語名 ````）で囲んでください。
    - 全体の文字数は1000文字程度を目安としてください。
    - SEOを意識したキーワード（指定されたテーマや関連語）を自然な形で記事全体にバランス良く含めてください。
- **ターゲット読者**: ITやプログラミングに初めて触れる方を想定し、専門知識がなくても理解できる内容にしてください。

## 指定内容
言語: {{言語名}}
テーマ: {{テーマ}}

## 出力例
```
---
title: Pythonの変数とデータ型入門｜初心者向け基本概念を徹底解説
date: 2025-09-20
categories: [Python, プログラミング入門]
tags: [Python, 変数, データ型, 初心者, プログラミング, 学習, AI, Gemini, 自動生成]
---

この記事はAIによって自動生成されています。

# Pythonの変数とデータ型とは？プログラミングの基礎を学ぼう！

「Pythonを始めたいけど、何から勉強すればいいの？」
「変数やデータ型ってよく聞くけど、一体何のこと？」

そんな疑問を持っているあなたに、Pythonプログラミングの超基本である「変数」と「データ型」について、やさしく丁寧に解説します。これらを理解すれば、あなたのプログラミング学習は大きく前進するはずです！

## 変数とは？情報をしまっておく「箱」

プログラミングにおける「変数（へんすう）」とは、一言で言うと**「データ（情報）を一時的にしまっておくための名前付きの箱」**のようなものです。この箱に、数字や文字などの色々なデータを自由に入れたり、取り出したり、中身を別のものに変えたりできます。

### 変数を使うメリット
-   **データの再利用**: 同じデータを何度も書かずに、変数名で呼び出せる
-   **コードの分かりやすさ**: 意味のある名前をつけることで、何を表すデータなのかが分かりやすくなる
-   **変更のしやすさ**: データが変わっても、変数の値を変えるだけで済む

### Pythonでの変数の使い方（サンプルコード）

Pythonで変数を使うのはとても簡単です。`変数名 = 値`という形で記述します。

```python
# 'name'という箱に「太郎」という文字を入れる
name = "太郎"

# 'age'という箱に「25」という数字を入れる
age = 25

# 'score'という箱に「85.5」という数字を入れる
score = 85.5

# 変数の中身を表示してみよう
print(name)  # 出力: 太郎
print(age)   # 出力: 25
print(score) # 出力: 85.5

# 変数の中身を後から変更することもできます
age = 26
print(age)   # 出力: 26
```
この例では、`name`、`age`、`score`がそれぞれ変数名です。それぞれに文字列、整数、小数という異なる種類のデータが入っていますね。

## データ型とは？「箱の中身の種類」

次に、「データ型（データがた）」について見ていきましょう。データ型とは、**変数の箱に入っているデータがどんな種類のものなのかを示す情報**です。

例えば、私たちは「数字」と「文字」を区別して使いますよね。プログラミング言語も同じように、データが「数字なのか」「文字なのか」などを区別して扱います。これがデータ型です。

Pythonには主に以下のような基本的なデータ型があります。

-   **整数型（`int`）**: 0, 1, -500 などの小数点を含まない数値
-   **浮動小数点型（`float`）**: 3.14, -0.5, 100.0 などの小数点を含む数値
-   **文字列型（`str`）**: "Hello", 'Python', "123" などの文字の並び
-   **真偽値型（`bool`）**: `True` (真) または `False` (偽) の2つの値

### なぜデータ型が重要なのか？

データ型を意識することは、プログラムが意図した通りに動作するために非常に重要です。例えば、数字と文字では、できる操作が異なります。

-   **数字**: 足し算、引き算、掛け算などの計算ができる
-   **文字**: 結合（文字と文字をつなげる）はできるが、足し算のような計算はできない

### データ型を確認してみよう（サンプルコード）

Pythonでは、`type()`関数を使って変数のデータ型を確認することができます。

```python
num = 100
text = "こんにちは"
is_active = True
price = 19.80

print(type(num))       # 出力: <class 'int'>
print(type(text))      # 出力: <class 'str'>
print(type(is_active)) # 出力: <class 'bool'>
print(type(price))     # 出力: <class 'float'>
```
このように、`type()`関数を使えば、変数がどのデータ型なのかが一目で分かります。

## まとめ：変数は「箱」、データ型は「箱の中身の種類」

今回はPythonプログラミングの基礎中の基礎である「変数」と「データ型」について学びました。

-   **変数**: データ（情報）をしまっておくための「名前付きの箱」
-   **データ型**: 箱の中に入っているデータがどんな「種類」なのか

これらは今後のプログラミング学習において、常に登場する重要な概念です。しっかりと理解することで、複雑なプログラムも読み解けるようになります。

まずは、Pythonの環境を用意して、実際に色々な変数を作成し、データ型を確認してみてください。手を動かすことが一番の近道です！
```
> **PR: ITスクールのご案内はこちら（アフィリエイトリンク）**

---
この記事はAI（Gemini API）によって自動生成されています。内容の正確性は保証されません。
---
